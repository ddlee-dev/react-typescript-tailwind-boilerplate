import tw from 'twin.macro';
import styled from 'styled-components/macro';

export const NavStyled = styled.nav(() => [
  // The common button styles added with the tw import
  tw`flex w-full h-14 bg-[#20232a]`
]);
